# Финальная передача Нурасылу — 23.09.2026

Сборка находится в feat/ui / PR №4. Финальное объединение в main выполняет Нурасыл; остальные не делают параллельный merge.

Предыдущий main: https://github.com/BAITC-Hacks/hack-024d5bf9-jjstan/commit/0835492ba01f6cbc954d207dd146b48f568fca01

## Состав

Финальная проверка: **152 passed, без пропусков**, включая оба реальных архива; **8 из 8** примеров engine успешны. Streamlit перезапущен из текущей feat/ui. В браузере проверены правка 72 → 84, утверждение, кнопка скачивания CSV, сброс состава и блокировка после смены срока. Проверка main остаётся за сборщиком после merge.

- Loader и его тесты/заметки совпадают с feat/data@5f1265b.
- Engine, примеры engine_examples.py и docs/engine_handoff.md включены через merge feat/engine@be75d23. Код engine остаётся cc15fe0.
- AI по умолчанию выключен. Ограничение сокращения issues не исправлено; живой API не проверен. Для основной защиты AI не включать.
- Приложенного нового README не было в сообщении: обновлён существующий README.

## Действия сборщика

1. Проверить чистое дерево, получить свежие ветки и проверить HEAD PR №4. При новых изменениях повторить проверки; не откатывать их к старым SHA.
2. Объединить полную сборку PR №4 в main. Ветка уже включает код зависимых PR; не заменять её старой веткой engine/data целиком.
3. В чистой локальной копии выполнить git fetch origin, git switch main, git pull --ff-only origin main. Проверить git log -1 и наличие app.py, engine_examples.py.
4. Установить requirements.txt, запустить pytest tests -q с STOCKPILOT_SOURCE_DIR, указывающим на папку обоих приватных архивов, затем python engine_examples.py. Архивы и ключи в Git не добавлять.
5. Остановить старый Streamlit через Ctrl+C, запустить python -m streamlit run app.py именно из обновлённой папки main. Обновить README: основной веткой запуска теперь является main.
6. Повторить демо: 72 → ручное количество 84 → подтверждение → CSV; изменить состав и срок поставки, убедиться в сбросе утверждения и требовании пересчёта. AI оставить выключенным.

При необходимости отката использовать revert проверенного merge/PR, без force push.

## Защита

Показать синтетический расчёт, формулу 140 + 20 − 50 − 40 = 70, округление до 72; правку, утверждение и CSV. Затем показать реальные выгрузки и конкретные причины блокировки. Последний сквозной отчёт: 3 909 insufficient, 0 допустимых заказов без подтверждённых условий. Полный сквозной расчёт в этом финальном этапе не повторяется, повторяется тест чтения обоих архивов.

BOM не реализован, совместимость с конкретным импортом 1С не подтверждена. Прототип не отправляет заказы поставщикам и не доказывает экономию или точность прогноза.

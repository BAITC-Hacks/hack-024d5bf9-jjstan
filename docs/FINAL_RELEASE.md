# Финальная сборка main — 23.09.2026

Нурасыл объединил PR №1, №3 и №4 в main. Merge-коммит полной сборки:
https://github.com/BAITC-Hacks/hack-024d5bf9-jjstan/commit/d317b2419aa5a373fd67b8faec6a9840efb33460
Дальнейший запуск команды — из обновлённой ветки main.

Предыдущий main: https://github.com/BAITC-Hacks/hack-024d5bf9-jjstan/commit/0835492ba01f6cbc954d207dd146b48f568fca01

## Состав

После merge повторён полный набор: **152 passed, без пропусков**, включая оба реальных архива (70,15 с). **8 из 8** примеров engine успешны. Все 28 файлов main сверены по Git blob SHA с проверенной сборкой. Дополнительно через Streamlit AppTest воспроизведены правка 72 → 84, утверждение, CSV с фактическим количеством 84, сброс состава и блокировка после смены срока. AI выключен по умолчанию. Локальный Streamlit запустился, health endpoint и главная страница ответили HTTP 200.

- Loader и его тесты/заметки совпадают с feat/data@5f1265b.
- Engine, примеры engine_examples.py и docs/engine_handoff.md включены через merge feat/engine@be75d23. Код engine остаётся cc15fe0.
- AI по умолчанию выключен. Ограничение сокращения issues не исправлено; живой API не проверен. Для основной защиты AI не включать.
- Приложенного нового README не было в сообщении: обновлён существующий README.

## Запуск у участников

1. Сохранить незавершённую локальную работу и выполнить git fetch origin, git switch main, git pull --ff-only origin main. Проверить git log -1 и наличие app.py, engine_examples.py.
2. Установить requirements.txt по README. Для проверки приватных архивов задать STOCKPILOT_SOURCE_DIR, указывающий на их локальную папку, выполнить python -m pytest tests -q и python engine_examples.py. Архивы и ключи в Git не добавлять.
3. Остановить старый Streamlit через Ctrl+C и выполнить python -m streamlit run app.py именно из обновлённой папки main.
4. Повторить демо: 72 → ручное количество 84 → подтверждение → CSV; изменить состав и срок поставки, убедиться в сбросе утверждения и требовании пересчёта. AI оставить выключенным.

При необходимости отката использовать revert проверенного merge/PR, без force push.

## Защита

Показать синтетический расчёт, формулу 140 + 20 − 50 − 40 = 70, округление до 72; правку, утверждение и CSV. Затем показать реальные выгрузки и конкретные причины блокировки. На финальном этапе повторён полный сквозной расчёт: 3 909 insufficient, 0 допустимых заказов, попытка выбрать все строки даёт 3 909 ошибок утверждения. Параметры проверки: 22.09.2026, срок/пересмотр/страховка 7/7/2 дня, аномалии и stockout выключены, тренд включён, подтверждения склада и условий заказа не заданы. Это параметры теста, а не установленные условия поставщиков. Время прогона 94,72 с — наблюдение на проверяющем ноутбуке.

BOM не реализован, совместимость с конкретным импортом 1С не подтверждена. Прототип не отправляет заказы поставщикам и не доказывает экономию или точность прогноза.

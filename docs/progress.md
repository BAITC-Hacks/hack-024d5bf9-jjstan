# Прогресс фронтенда — Рамазан

## 23.09.2026 · текущая интеграция и возобновление публикации

- Рамазан отдельно поручил push текущей работы: публикация в feat/ui разрешена, main не меняем.
- После сравнения импортированы ровно три файла Нурасыла из StockPilot-data-reconciliation.zip без правок содержимого.
- Подключён engine Гапара cc15fe0; UI использует ENGINE_CAPABILITIES, настоящие переключатели тренда/аномалий/stockout и показывает anomalies выбранного SKU.
- В общую сборку входят ранее локальные исправление XLSX, AI-пояснения, безопасный кэш и mock-тесты. Живой AI-запрос пока не проверен.
- Тесты: 151 passed, 1 skipped. Сквозная проверка обоих реальных архивов: 3 909 товаров, 0 допустимых рекомендаций без подтверждённых условий, запрет утверждения сохранён. Детали в integration_review.md.
- Записи ниже относятся к прежним этапам и прежним версиям engine.

## 23.09.2026 · первая рабочая версия

- В `feat/ui` подключены `origin/main`, `origin/feat/data` с loader и актуальный `origin/feat/engine` (`0929436`); файлы тиммейтов не редактировались.
- Созданы три вкладки: заказы, объяснение SKU, данные и диагностика.
- Добавлены загрузка ZIP/XLSX, явный деморежим, настройки поставщиков и категорий.
- Реализованы правки по ключу SKU, проверка минимума/кратности, утверждение снимка, сброс при изменениях и CSV.
- Текущие неподдерживаемые возможности engine показаны и выключены.
- Локальные проверки engine/loader: 52 passed, 1 skipped; отдельно реальный тест архивов: 1 passed.
- Проверки нашей части: 26 passed. Общий проход: 78 passed, 1 skipped (реальные архивы проверены отдельно).
- Для нового engine добавлены явные подтверждения склада и недостающих условий MOQ. Настройки отключены по умолчанию и не заменяют данные файлов.
- Сквозная проверка реальных данных: 3 909 товаров, рекомендации заблокированы диагностикой исходных данных; подробности в `integration_review.md`.
- Проверка в браузере: отображение демоданных, выбор и утверждение четырёх позиций; визуальная проверка таблицы и адаптация узкого окна.

Проверить: установить `requirements.txt`, выполнить `python -m pytest -q`, запустить `python -m streamlit run app.py`. Коммиты фронтенда находятся в `feat/ui`; `main` этой работой не обновляется.

## 23.09.2026 · задание Нурасыла: локальный этап XLSX / AI

- Git на паузе: новых commit/push/merge не выполнялось. HEAD до и после работы — `72a4f86`, удалённые ветки также не изменились.
- Проверены инструкции и локальные правки перед работой: рабочее дерево было чистым; дополнительных AGENTS.md в репозитории/родительских каталогах не найдено.
- Исправлен одиночный XLSX без имени поставщика: выбор IEK/Systeme Electric передаётся в безопасную временную папку и входит в подпись расчёта. Нельзя вывести поставщика из названия товара.
- Реальный IEK XLSX с месячными продажами: **2 463 товара, 81 279 строк продаж, 0 unrecognized_supplier_or_file_type**. Без выбора показана понятная ошибка; исходный архив не изменён.
- Добавлены `ai_assistant.py`, mock-тесты, предложение `docs/ai_contract.md` для Гапара, два действия в карточке SKU и полная локальная диагностика товара.
- AI использует официальный OpenAI SDK / Responses API. Модель выбирает проверенные тезисы/вопросы; числа и текст собираются локально, произвольные команды модели не исполняются и количества не перезаписываются.
- Проверены отсутствие ключа, timeout, невалидный JSON, неподдержанный контекст, чужие evidence_id, недоступная модель, единственный активный запрос, максимум один временный повтор, кэш и сохранение approval/CSV.
- Финальный общий прогон: **112 passed, 1 skipped** за 22,75 с. Пропущен только условный тест полных приватных архивов, ранее пройденный отдельно; реальный одиночный IEK XLSX проверен в этом этапе. pip check без ошибок.
- В браузере проверены обычная формула 72, безопасное сообщение без ключа, карточка insufficient и первая причина блокировки.
- Live smoke-test ещё не выполнен: локальные OPENAI_API_KEY/OPENAI_MODEL не обнаружены на момент записи. Никаких платных вызовов во время тестов не было.
- Нового engine/сообщения о готовности флагов нет: `feat/engine@0929436`. Аномалии/stockout остаются отключены, тренд не имитируется. Чужие модули, тесты и методика не редактировались.
- NVIDIA inference/Brev не подключались; баланс и купон не проверены. Нового сервера/БД/фронтенда не создавалось.

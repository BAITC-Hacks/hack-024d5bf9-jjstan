# Адаптеры данных StockPilot — Нурасыл

```python
from data_loader import load_data
dataset = load_data(["/path/to/Systeme electric.zip", "/path/to/IEK.zip"])
```

Python 3.10+, pandas, numpy, openpyxl; тесты: `python -m pytest tests/test_data.py -q`.
Исходники остаются локально. ZIP читается в памяти без распаковки; можно передать XLSX
с исходными именами и папками поставщиков. Все 8 таблиц контракта возвращаются всегда.
Синтетический сквозной пример находится в `tests/test_data.py`.

## Для интеграции

- **Расхождение контракта:** прямое задание Нурасыла задаёт `*all*`, TEAM_PLAN.md — `__all__`.
  Loader следует прямому заданию: `data_loader.ALL == "*all*"`. Координатору нужно
  согласовать маркер с engine; обязательные колонки не изменены.
- Сначала подключайте Systeme Electric. Дата снимка берётся из имени файла транзита.
  `is_current=True` означает актуальный снимок **на указанную as_of**, а не на день запуска.
  Engine должен отдельно проверять возраст снимка относительно settings.as_of.
- `quality_report.severity` содержит `info`, `warning`, `error`; issue — стабильный код.
- `stock_quantity`, `raw_quantity`, `stock_basis`, `report_month`, `snapshot_as_of`,
  `date_basis`, `unit_basis`, `requires_unit_conversion`, `order_constraint_raw`, `source`
  — дополнительные поля происхождения данных. Они не меняют обязательный контракт.
  `unit_basis=sku_reference` означает единицу из точного сопоставления SKU, а не явно
  указанную в каждой строке транзита единицу. Конверсия между единицами не выполняется.
- `monthly_sales` и `transactions` нельзя складывать. Для общего месячного прогноза
  основной источник — отдельный месячный отчёт. Динамика нужна для детализации/аномалий.
- Сентябрь 2026 неполный. При известной дате отчёта полными считаются только закончившиеся
  месяцы; иначе последний представленный месяц консервативно неполный.

## Правила источников

- Строковый код 1С сохраняет нули, подчёркивания и кириллицу. Числовые коды с Excel-форматом
  `00000` восстанавливаются по этому формату; без него потерянные нули не угадываются.
- Колонки/строки итогов исключаются. Пустые месячные клетки остаются NaN, а не нулём.
- Идентичный файл ZIP/XLSX распознаётся по содержимому и поставщику. Повторные одинаковые
  месяцы/остатки/поставки сохраняются один раз; конфликтующие ключи исключаются с error.
  Пересекающиеся документы разных файлов динамики исключаются с error: ID строки документа нет.
  Одинаковые строки внутри одного файла не удаляются автоматически — это могут быть разные
  строки одной накладной. При нескольких датированных снимках активен последний снимок
  поставщика; предыдущие остатки исторические, предыдущий транзит не суммируется с новым.
- В этих выгрузках расходная накладная сохраняет исходный знак: `quantity=raw_quantity`.
  Отрицательная накладная — возврат/сторно, положительная — продажа. Нельзя разворачивать
  всю таблицу по нескольким отрицательным строкам. Явный возврат от покупателя получает
  отрицательный знак. Неизвестный документ даёт NaN, raw_quantity
  сохраняется. Суммы сверяются с месячным отчётом по совпадающим SKU и месяцам.
  При неизвестной операции/количестве сумма месяца не считается полной. Различия охвата
  истории и неполнота сверки имеют отдельные записи в quality_report.
- Свободный остаток Systeme Electric берётся напрямую; резерв повторно не вычитается,
  складские составляющие к общей величине не прибавляются. Общий снимок помечен `*all*`.
  Точное соответствие охвата месячной истории и снимка требует подтверждения.
- Исторический IEK — начало месяца: as_of=первое число, is_current=False.
  Исторический Systeme Electric не сообщает день снимка: as_of=NaT, report_month сохранён.
  Ни один исторический отчёт не доказывает свободный остаток после резерва:
  исходное число хранится в `stock_quantity`, `free_stock=NaN`.
- IEK ожидаемые даты — «поступление до» в заголовке, date_basis=arrival_deadline.
  У Systeme Electric 24.09 дополнено годом из имени файла. Это дата заголовка,
  date_basis=header_date_unconfirmed; её смысл как ETA нужно подтвердить.
  Неизвестные склады транзита остаются null. Пустая клетка транзита не доказывает нулевое поступление.
- Бухты/метры: никакого умножения на 305 по названию. При явном предупреждении источника
  `requires_unit_conversion=True`, quantity=NaN, raw_quantity сохранено, ограничения MOQ
  не применимы до подтверждения единиц.
- Systeme MOQ «Кратность» → pack_multiple; IEK «Мин. разр. к отгр.» → min_order_qty.
  Второе ограничение остаётся NaN. Единицы ограничений в MOQ отдельно не указаны.
- Сезонность берётся из готовой колонки «СЕЗОННОСТЬ» отдельного файла с category=`*all*`.
  Встроенные копии листов игнорируются. Профиль учитывает неполный 2026; мера продаж
  и применимость к штукам/метрам не подтверждены. Loader не пересчитывает прогноз.
- Категория 2026 сохраняется строкой без присвоения бизнес-смысла. Кэф. Роста и Кэф. Сез-ти
  сохраняются как growth_coefficient_raw / seasonality_coefficient_raw: это не подтверждённые
  множители прогноза; например, рост может быть отрицательной долей изменения.

## Отсутствует

Подтверждённая ежедневная stockout-история, customer_id, полный справочник сроков поставки,
отдельная BOM, актуальный остаток IEK. Stockouts возвращается пустым; customer_id=null.
Отсутствие движения не превращается в stockout. Номер накладной не является клиентом.
Неизвестные единицы, конфликты справочников и SKU только одного источника отражаются
в quality_report. Рекомендации при недостатке обязательных данных должен блокировать engine.

## Проверено на предоставленных архивах

| Таблица | Systeme Electric | IEK |
|---|---:|---:|
| products | 724 | 3 185 |
| monthly_sales | 18 282 | 81 279 |
| transactions | 77 312 | 171 603 |
| stock (включая историю) | 23 630 | 94 149 |
| transit (включая явные нули) | 497 | 306 |
| seasonality | 12 | 12 |

Systeme Electric: 497 свободных остатков на 22.09.2026; у 23 из этих SKU единица
не найдена в других источниках. IEK: только исторические начальные остатки.
В динамике 408 отрицательных расходных накладных, 30 строк с отсутствующим количеством
и 9 строк других типов документов, которые не объявлены продажами.

Сверка только численно полных совпадающих ключей (SKU, месяц): Systeme Electric —
2 668 совпадений и 4 337 расхождений; IEK — 14 504 совпадения и 7 726 расхождений.
Разворот знака всей динамики не исправляет эти расхождения. Охват истории/складов
не подтверждён; нельзя считать источники полностью сверенными или объединять их суммы.
Пустые месячные клетки не принимались за нулевые продажи даже ради улучшения сверки.

Полный локальный интеграционный тест (путь каждый участник задаёт сам):

```powershell
$env:STOCKPILOT_SOURCE_DIR = "C:/path/to/private/data"
python -m pytest tests/test_data.py -q
```

Без этой переменной реальные архивы не требуются: интеграционный тест пропускается,
синтетические тесты проверяют контракт, знаки, даты, единицы, повторные источники,
MOQ, сезонность, резервы и недостающие данные. Исходники тестов не содержат выгрузок.

## Уточнение сверки для интеграции, 23.09.2026

В непустой `monthly_sales` добавлены необязательные поля:

- `reconciled_transaction_quantity` — сумма нормализованных движений того же
  supplier/SKU/месяца, NaN при отсутствии движений или неизвестном количестве;
- `reconciliation_difference` — monthly quantity минус сумма движений, NaN при
  неполной паре. Исходная `quantity` не изменяется;
- `reconciliation_status`: `matched`, `mismatch`, `incomplete_quantity`,
  `no_transaction_rows` или `transactions_unavailable` (таблица движений пуста).

`matched` означает численное совпадение доступных значений, а не подтверждение
складского охвата, полноты документов или пригодности заказа. Отсутствие строк
динамики не доказывает нулевой спрос. Нулевой итог известных продаж и возвратов
отличается от отсутствующей истории.

Для `monthly_transaction_mismatch:...` в quality_report добавлены `affected_tables`,
`affected_months` (точный список YYYY-MM через запятую), `period_start` и `period_end`
(datetime). Последние два поля — внешние границы, внутри возможны месяцы без ошибок.
При проверке применимости нужно использовать точный список и объединение всех периодов,
которые читают базовый прогноз, тренд, аномалии, калибровка и другие включённые алгоритмы.
Loader не меняет severity и не разрешает игнорировать ошибки.

На неизменных исходных архивах найдены 36 товаров Systeme Electric с датированным
свободным остатком, для которых июнь, июль и август 2026 совпадают одновременно в
отдельном месячном отчёте, динамике и встроенной истории файла транзита (108 пар).
Две месячные таблицы могут быть копиями одного отчёта: это не независимое подтверждение
бизнес-смысла. Для периода март–август среди товаров с текущим остатком таких полных
совпадений нет. Нельзя молча сократить историю до трёх месяцев, чтобы снять блокировки:
это отдельная явно выбранная методика с ограничениями короткой истории.

Складской охват, единицы MOQ, отсутствие отдельного минимума, параметры сроков поставки
и применимость сезонности остаются неподтверждёнными. Срез остатков относится к 22.09.2026,
а не автоматически к дате запуска. IEK по-прежнему не имеет текущего свободного остатка.
Локальный список SKU с адресами ячеек передаётся отдельно и не является allowlist для заказа.

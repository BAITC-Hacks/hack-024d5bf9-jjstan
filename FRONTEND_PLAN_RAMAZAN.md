# Рамазан — план фронтенда StockPilot на 5 часов

## Цель твоей работы

Собрать понятный интерфейс закупщика и соединить работу команды: файлы → расчёт → просмотр причин → изменение количества → утверждение → скачивание заказа.

Нурасыл отвечает за `data_loader.py`, Гапар — за `engine.py`. Ты отвечаешь за приложение, состояние интерфейса, экспорт, общую сборку и демонстрацию. Формулы заказа и разбор Excel остаются в модулях тиммейтов.

План согласован с `TEAM_PLAN.md` в репозитории `BAITC-Hacks/hack-024d5bf9-jjstan`. Это план дальнейшей разработки: приложение ещё не создано в рамках этого документа.

## 1. Где работаем и что используем

| Инструмент | Для чего |
|---|---|
| VS Code | Редактирование файлов проекта и встроенный терминал PowerShell |
| Python 3.12 | Предлагаемая версия для единого окружения команды; если у команды уже согласована другая совместимая версия, используем её |
| `.venv` | Изолированное окружение проекта |
| Streamlit | Интерфейс приложения на Python |
| pandas | Таблицы, поступающие от тиммейтов |
| openpyxl | Зависимость чтения Excel и возможного XLSX-экспорта |
| pytest | Расчётные проверки команды и проверки экспорта |
| Браузер | Просмотр локального приложения, обычно `http://localhost:8501` |
| Git и GitHub | Ветка `feat/ui`, объединение модулей, почасовые пуши |

Отдельный сервер, React, Node.js, база данных и макет в Figma для этого MVP не требуются. Приложение показываем локально на ноутбуке. Публикацию сайта рассматриваем только после готовности основного сценария.

**Рабочая папка:** отдельный клон общего репозитория. Предлагаемое расположение на твоём компьютере: `C:\Users\ramaz\Documents\Codex\stockpilot`. Если клон уже существует, используем его — второй не создаём.

### Подготовка в PowerShell

Проверь, что `git --version` и `py --version` работают. Если команды отсутствуют, установи Git for Windows и Python с официальных сайтов, затем открой новый терминал. Установка может потребовать доступ к сети и обычную авторизацию GitHub.

Для первого клонирования, если предложенная папка ещё не занята:

```powershell
Set-Location 'C:\Users\ramaz\Documents\Codex'
git clone https://github.com/BAITC-Hacks/hack-024d5bf9-jjstan.git stockpilot
Set-Location '.\stockpilot'
git switch -c feat/ui
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install streamlit pandas numpy openpyxl pytest
```

Если `feat/ui` уже есть локально, используй `git switch feat/ui`. Если есть только `origin/feat/ui`, после `git fetch origin` используй `git switch --track origin/feat/ui`. При существующем согласованном `requirements.txt` устанавливай зависимости через `-m pip install -r requirements.txt` вместо создания другого набора.

Команды через `.venv\Scripts\python.exe` не требуют активации PowerShell-скрипта. В VS Code открой папку `stockpilot` и выбери этот Python как интерпретатор.

Когда создан `app.py`, запуск:

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py
```

Открой адрес, который напечатает терминал. Для остановки — Ctrl+C. После проверенного запуска зафиксируй совместимые версии зависимостей в общем `requirements.txt` и укажи версию Python в README.

## 2. Какие файлы создаёшь ты

```text
stockpilot/
├── TEAM_PLAN.md             общий договор команды
├── app.py                   твой интерфейс и управление состоянием
├── export.py                подготовка CSV утверждённого заказа
├── demo_data.py             явно синтетический набор для раннего UI
├── requirements.txt         общие зависимости
├── .gitignore               исключения для Git
├── .streamlit/
│   └── config.toml          цвета и базовая тема
├── tests/
│   └── test_export.py       проверка утверждённых количеств и экспорта
├── docs/
│   └── progress.md          почасовой статус команды
├── data/
│   └── raw/                реальные архивы только локально
├── data_loader.py           модуль Нурасыла
└── engine.py                модуль Гапара
```

Это предлагаемая структура будущего репозитория. Если тиммейты уже добавили файлы, сначала проверь их содержимое. Не перезаписывай общие файлы и не меняй имена функций без согласования.

В `.gitignore` добавь `.venv/`, `__pycache__/`, `.pytest_cache/`, `.env`, `.streamlit/secrets.toml`, `data/raw/` и каталог временных загрузок. Настройки темы можно коммитить, секреты — нельзя.

## 3. Какие данные используешь

### Сначала: синтетический пример

Сделай 6–10 товаров по формату из `TEAM_PLAN.md`, а не произвольный JSON. Нужны разные состояния:

- обычный заказ, например расчёт 70 → 72 с округлением;
- достаточный остаток и нулевой заказ;
- риск дефицита;
- ожидаемая поставка;
- отмеченная разовая аномалия;
- отсутствующий актуальный остаток;
- два поставщика, чтобы проверить фильтр и группировку.

`demo_data.py` возвращает тестовые входные данные и, пока engine не готов, подписанные демонстрационные результаты того же контракта. В UI постоянно видно «Демонстрационные данные». После готовности engine тестовый набор тоже пропускается через настоящий расчёт.

### Затем: реальные архивы

На твоём компьютере ранее были предоставлены:

- `C:\Users\ramaz\Downloads\Systeme electric.zip`;
- `C:\Users\ramaz\Downloads\IEK.zip`.

Можно выбрать их через интерфейс загрузки или хранить копии в `data/raw/`. Для проверки сначала используй Systeme Electric, затем IEK. Сами Excel разбирает Нурасыл.

Загруженные через Streamlit байты сохраняй во временную папку текущей сессии под безопасными именами и передавай loader список путей. Для MVP принимай ZIP и XLSX; рекурсивную распаковку и нормализацию не дублируй во фронтенде. Не записывай клиентский путь как путь назначения на сервере.

Не подменяй реальные данные синтетическими при ошибке. Показывай причину ошибки и оставляй переключение в деморежим явным действием пользователя.

## 4. Какие экраны делаем

Одно приложение: боковая панель и три вкладки. В верхней части — название StockPilot, дата расчёта и статус источников. Светлый фон, один основной цвет, красный для ошибок, жёлтый для ограничений; значения статусов дублируются текстом.

### Боковая панель — ввод и настройки

- Режим: демонстрационные / реальные данные.
- Загрузка ZIP/XLSX.
- Дата расчёта, согласованная с датами источников.
- Срок поставки отдельно для каждого поставщика.
- Период между пересчётами и страховые дни категорий.
- Переключатели аномалий и stockout.
- Кнопка «Рассчитать заказ».

Не выдавай неизвестный срок поставки за подтверждённый. Отсутствующие интервалы stockout должны сопровождаться понятным сообщением. Настройки меняются в форме; дорогой расчёт запускается по кнопке, а не от каждого изменения фильтра.

### Вкладка «Заказы»

Сверху: количество позиций к заказу, срочных позиций и позиций с проблемами данных. Не суммируй штуки и метры в один показатель и не показывай стоимость закупки без подтверждённых закупочных цен.

Таблица:

| Поле на экране | Источник |
|---|---|
| Поставщик | `supplier` |
| Код и название | `sku`, `name` |
| Единица | `unit` |
| Свободный остаток | `free_stock` |
| В пути на горизонте | `incoming_in_horizon` |
| Рекомендовано | `recommended_qty` |
| Количество к утверждению | UI-поле `approved_qty` |
| Срочность | `urgency` |
| Причина | `reason` |
| Качество данных | `data_quality` |

Добавь поиск по SKU/названию и фильтр по поставщику. Редактируются только количество к утверждению и выбор строк. Остальные поля доступны для чтения.

Кнопки: «Утвердить выбранные позиции» и «Скачать утверждённый заказ». Рядом явно показывай количество выбранных строк и поставщиков. Фильтр не должен незаметно менять состав утверждаемого заказа.

### Вкладка «Объяснение SKU»

Выбор товара, график истории и прогноза, список исключённых аномалий, дата остатка, ожидаемые поступления, числовые слагаемые формулы и причина округления. Исторические месячные продажи и дневной прогноз перед показом приводи к сопоставимой частоте; неполные месяцы помечай.

Расчёт берётся у Гапара. Если нужной детализации нет в контракте, согласуй дополнительное поле; не создавай вторую версию алгоритма ради карточки.

### Вкладка «Данные»

Загруженные файлы, дата источников, число обработанных товаров, таблица `quality_report` и `diagnostics`. Для пустого результата, загрузки, ошибки, устаревшего остатка и отсутствующего engine предусмотрены отдельные понятные состояния.

## 5. Как подключаем тиммейтов

```python
from data_loader import load_data
from engine import calculate_orders

dataset = load_data(paths)
result = calculate_orders(dataset, settings)

orders = result["orders"]
forecast = result["forecast"]
anomalies = result["anomalies"]
diagnostics = result["diagnostics"]
```

Точные поля таблиц и настроек уже записаны в `TEAM_PLAN.md`. Не переименовывай их для русских заголовков — русские названия задаются при отображении.

**От Нурасыла нужно:** loader, совместимые таблицы, даты и статус актуальности остатков, сообщение о пропусках.

**От Гапара нужно:** engine, стабильные четыре таблицы результата, причины расчёта, пример с известным ответом.

До готовности модулей UI работает с явно обозначенной заглушкой. Отсутствие конкретного модуля можно обработать отдельно; любые внутренние ошибки импорта нельзя скрывать общим fallback на демо.

## 6. Состояние, утверждение и экспорт

Streamlit перезапускает скрипт при взаимодействии с элементами. Сохраняй результат расчёта, ручные количества и утверждение в `st.session_state`.

Правки храни по `(supplier, sku)`, не по номеру строки. При фильтрации и сортировке проверяй соответствие редактора стабильным ключам: сам факт использования `data_editor` не решает перенос правок между строками.

Состояния заказа: **нет расчёта → черновик → утверждён → экспортирован**. Изменение исходных файлов, параметров расчёта или ручного количества отменяет прежнее утверждение. Пока новый расчёт не выполнен, старый результат помечается как устаревший и не утверждается.

Перед утверждением проверяй неотрицательность, конечность числа, единицу и допустимость количества для товара. Минимум/кратность проверяются по правилам поставщика. Не округляй все товары до целого: для части единиц могут быть дробные количества. Проверку исключений из правил согласуй с командой; молча не пропускай.

Строки `data_quality="insufficient"` не включай в утверждённый заказ до устранения обязательных пропусков. Нулевое ручное значение допустимо как отказ от закупки позиции и не заменяется рекомендацией автоматически.

CSV: поставщик, код 1С, артикул поставщика при наличии, название, единица, утверждённое количество, исходная рекомендация, причина. Используй UTF-8 с BOM для удобства открытия кириллицы в Excel. Для внешних текстовых полей предотврати интерпретацию строк как формул при открытии в таблицах. Экспорт содержит снимок утверждённых данных, а не заново рассчитанные значения.

Состояние сессии не является постоянной базой: после перезапуска оно может потеряться. Подготовленный заказ пользователь скачивает. CSV с кодом 1С не называем готовой интеграцией с 1С без проверки формата импорта.

## 7. Твои пять часов

| Время | Что делаешь | Проверяемый результат |
|---|---|---|
| 0:00–0:15 | Открываешь репозиторий, подтверждаешь контракт, окружение и ветку | Все понимают имена функций и поля |
| 0:15–1:00 | `app.py`, тема, три вкладки, тестовые товары | Интерфейс запускается, демоданные видны; push №1 |
| 1:00–2:00 | Загрузка файлов, настройки, подключение первых версий loader/engine | Сквозной расчёт Systeme Electric; push №2 |
| 2:00–3:00 | Правки, состояния, утверждение, CSV; карточка SKU | Менеджер скачивает выбранный утверждённый заказ; push №3 |
| 3:00–4:00 | IEK через loader, диагностика, проверки фильтров и экспорта, README | Оба поставщика отображаются с ограничениями данных; push №4 |
| 4:00–4:30 | Проверка чистого запуска, исправления, сборка команды | Стабильная версия |
| 4:30–5:00 | Репетиция, резервная запись демо, финальный README и push | Готовая демонстрация; push №5 |

При нехватке времени сначала сокращаем оформление, дополнительные графики и XLSX. Сохраняем загрузку, реальные рекомендации, объяснение, правку, утверждение и CSV.

## 8. Git и интеграция

Твоя ветка — `feat/ui`. До :45 каждого часа все пушат свои ветки. Ты интегрируешь готовые PR в :45–:55, проверяешь запуск и затронутые тесты; до :59 результат должен быть в `main`. Не сливай сломанный модуль ради часовой отметки: рабочая ветка и статус показывают прогресс отдельно.

Коммит только существующих проверенных файлов, например:

```powershell
git status
git diff
git add app.py export.py README.md
git diff --cached
git commit -m "feat: add order review and approved CSV export"
git push origin feat/ui
```

В `docs/progress.md` фиксируй результат часа и ссылки на коммиты всей команды. Перед дальнейшей работой синхронизируй свою ветку с актуальным `main` обычным merge; конфликты в чужих модулях решай вместе с владельцем.

## 9. Что проверяем перед защитой

- [ ] Приложение запускается по README в окружении с указанными зависимостями.
- [ ] Деморежим заметно отличается от реальных данных.
- [ ] Загрузка не требует редактирования путей в исходном коде.
- [ ] Отсутствующий engine или некорректный файл дают понятное сообщение.
- [ ] Фильтрация и сортировка не переносят ручное количество на другой SKU.
- [ ] Изменение параметров отменяет старое утверждение.
- [ ] Ручной ноль сохраняется; некорректное количество не утверждается.
- [ ] CSV содержит именно выбранные утверждённые значения и корректную кириллицу.
- [ ] Неизвестный текущий остаток не отображается как нулевой.
- [ ] Не складываются количества в разных единицах.
- [ ] Есть резервный демонстрационный набор на случай сбоя загрузки.

## 10. Документация для реализации

- [Streamlit: установка и запуск](https://docs.streamlit.io/get-started/installation/command-line).
- [Streamlit: редактируемая таблица](https://docs.streamlit.io/develop/api-reference/data/st.data_editor).
- [Streamlit: Session State](https://docs.streamlit.io/develop/api-reference/caching-and-state/st.session_state).
- [Общий план команды](https://github.com/BAITC-Hacks/hack-024d5bf9-jjstan/blob/main/TEAM_PLAN.md).
